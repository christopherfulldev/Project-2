/*import axios from "axios";

const newRequestGames = async () => {
    
    const token = '0f863245b63641b3903f13e17e029a8c';
    api.defaults.headers.authorization = `Bearer ${token}`;

    await axios.get('https://api.rawg.io/api/games?page_size=5&search=gta%20v')
    .then(response => {
        const games = response.data;
    })
    return response;
  }

  export default newRequest;*/